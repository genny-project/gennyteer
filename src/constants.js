import UUID from 'short-uuid';

export const JOB_ID = `TEST_LOAD_${( new UUID ).new()}`;
export const TEST_TIMEOUT = 10000;
export const PAGE_WIDTH = 1920;
export const PAGE_HEIGHT = 1080;
export const SECONDS = 1000;